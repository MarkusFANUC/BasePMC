//:--------------------------------------------------------------------------:
//: File              : tool.src                                             :
//: Creation date     : 16.09.2013                                           :
//: Last modification : see table below                                      :
//: Author            : FANUC FA Switzerland / Peter Bloesch                 :
//: Description       : tool management                                      :
//:--------------------------------------------------------------------------:
//
//:--------------------------------------------------------------------------:
//:                     Modification                                         :
//:--------------------------------------------------------------------------:
//: Date   :Version: Sign  :Description                                      :
//:--------------------------------------------------------------------------:
//:25.10.16: 2.0.0 :Blp    :path 11-15 added                                 :
//:--------------------------------------------------------------------------:
//:        :       :       :                                                 :
//:--------------------------------------------------------------------------:
//:        :       :       :                                                 :
//:--------------------------------------------------------------------------:
//:        :       :       :                                                 :
//:--------------------------------------------------------------------------:

// ===========================================================================
// Code
// ===========================================================================

#if t_code_support_P01 > 0
// ---------------------------------------------------------------------------
// P01:tool management
// ---------------------------------------------------------------------------
   #if net_comment == 1
(*
P01:tool management
*)
   #endif
                 CALLU     Tool_P01             ;"P01:tool management"
#endif

#if nbr_of_path > 1
   #if t_code_support_P02 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P02:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P02:tool management
*)
      #endif
                 CALLU     Tool_P02             ;"P02:tool management"
   #endif
#endif

#if nbr_of_path > 2
   #if t_code_support_P03 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P03:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P03:tool management
*)
      #endif
                 CALLU     Tool_P03             ;"P03:tool management"
   #endif
#endif

#if nbr_of_path > 3
   #if t_code_support_P04 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P04:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P04:tool management
*)
      #endif
                 CALLU     Tool_P04             ;"P04:tool management"
   #endif
#endif

#if nbr_of_path > 4
   #if t_code_support_P05 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P05:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P05:tool management
*)
      #endif
                 CALLU     Tool_P05             ;"P05:tool management"
   #endif
#endif

#if nbr_of_path > 5
   #if t_code_support_P06 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P06:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P06:tool management
*)
      #endif
                 CALLU     Tool_P06             ;"P06:tool management"
   #endif
#endif

#if nbr_of_path > 6
   #if t_code_support_P07 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P07:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P07:tool management
*)
      #endif
                 CALLU     Tool_P07             ;"P07:tool management"
   #endif
#endif

#if nbr_of_path > 7
   #if t_code_support_P08 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P08:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P08:tool management
*)
      #endif
                 CALLU     Tool_P08             ;"P08:tool management"
   #endif
#endif

#if nbr_of_path > 8
   #if t_code_support_P09 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P09:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P09:tool management
*)
      #endif
                 CALLU     Tool_P09             ;"P09:tool management"
   #endif
#endif

#if nbr_of_path > 9
   #if t_code_support_P10 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P10:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P10:tool management
*)
      #endif
                 CALLU     Tool_P10             ;"P10:tool management"
   #endif
#endif

#if nbr_of_path > 10
   #if t_code_support_P11 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P11:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P11:tool management
*)
      #endif
                 CALLU     Tool_P11             ;"P11:tool management"
   #endif
#endif

#if nbr_of_path > 11
   #if t_code_support_P12 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P12:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P12:tool management
*)
      #endif
                 CALLU     Tool_P12             ;"P12:tool management"
   #endif
#endif

#if nbr_of_path > 12
   #if t_code_support_P13 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P13:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P13:tool management
*)
      #endif
                 CALLU     Tool_P13             ;"P13:tool management"
   #endif
#endif

#if nbr_of_path > 13
   #if t_code_support_P14 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P14:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P14:tool management
*)
      #endif
                 CALLU     Tool_P14             ;"P14:tool management"
   #endif
#endif

#if nbr_of_path > 14
   #if t_code_support_P15 > 0 && t_code_support_P01 > -1
// ---------------------------------------------------------------------------
// P15:tool management
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P15:tool management
*)
      #endif
                 CALLU     Tool_P15             ;"P15:tool management"
   #endif
#endif
