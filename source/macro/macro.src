//:--------------------------------------------------------------------------:
//: File              : macro.src                                            :
//: Creation date     : 03.09.2014                                           :
//: Last modification : see table below                                      :
//: Author            : FANUC FA Switzerland / Peter Bloesch                 :
//: Description       : call of path specific one touch macro subprograms    :
//:--------------------------------------------------------------------------:
//
//:--------------------------------------------------------------------------:
//:                     Modification                                         :
//:--------------------------------------------------------------------------:
//: Date   :Version: Sign  :Description                                      :
//:--------------------------------------------------------------------------:
//:20.10.16: 2.0.0 :Blp    :path 11-15 added                                 :
//:--------------------------------------------------------------------------:
//:        :       :       :                                                 :
//:--------------------------------------------------------------------------:
//:        :       :       :                                                 :
//:--------------------------------------------------------------------------:
//:        :       :       :                                                 :
//:--------------------------------------------------------------------------:

// ===========================================================================
// Code
// ===========================================================================

#if otmc_support_P01 > 0
// ---------------------------------------------------------------------------
// P01:one touch macro call
// ---------------------------------------------------------------------------
   #if net_comment == 1
(*
P01:one touch macro call
*)
   #endif
                 CALLU     OneTouchMac_P01      ;"P01:one touch macro call"
#endif

#if nbr_of_path > 1
   #if otmc_support_P02 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P02:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P02:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P02      ;"P02:one touch macro call"
   #endif
#endif

#if nbr_of_path > 2
   #if otmc_support_P03 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P03:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P03:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P03      ;"P03:one touch macro call"
   #endif
#endif

#if nbr_of_path > 3
   #if otmc_support_P04 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P04:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P04:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P04      ;"P04:one touch macro call"
   #endif
#endif

#if nbr_of_path > 4
   #if otmc_support_P05 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P05:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P05:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P05      ;"P05:one touch macro call"
   #endif
#endif

#if nbr_of_path > 5
   #if otmc_support_P06 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P06:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P06:cone touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P06      ;"P06:one touch macro call"
   #endif
#endif

#if nbr_of_path > 6
   #if otmc_support_P07 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P07:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P07:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P07      ;"P07:one touch macro call"
   #endif
#endif

#if nbr_of_path > 7
   #if otmc_support_P08 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P08:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P08:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P08      ;"P08:one touch macro call"
   #endif
#endif

#if nbr_of_path > 8
   #if otmc_support_P09 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P09:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P09:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P09      ;"P09:one touch macro call"
   #endif
#endif

#if nbr_of_path > 9
   #if otmc_support_P10 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P10:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P10:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P10      ;"P10:one touch macro call"
   #endif
#endif

#if nbr_of_path > 10
   #if otmc_support_P11 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P11:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P11:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P11      ;"P11:one touch macro call"
   #endif
#endif

#if nbr_of_path > 11
   #if otmc_support_P12 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P12:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P12:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P12      ;"P12:one touch macro call"
   #endif
#endif

#if nbr_of_path > 12
   #if otmc_support_P13 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P13:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P13:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P13      ;"P13:one touch macro call"
   #endif
#endif

#if nbr_of_path > 13
   #if otmc_support_P14 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P14:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P14:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P14      ;"P14:one touch macro call"
   #endif
#endif

#if nbr_of_path > 14
   #if otmc_support_P15 > 0 && otmc_support_P01 > -1
// ---------------------------------------------------------------------------
// P15:one touch macro call
// ---------------------------------------------------------------------------
      #if net_comment == 1
(*
P15:one touch macro call
*)
      #endif
                 CALLU     OneTouchMac_P15      ;"P15:one touch macro call"
   #endif
#endif
